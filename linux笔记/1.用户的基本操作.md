# 用户的基本操作
- 添加一个用户 ：useradd（管理员可使用这个命令，普通用户不可使用）
	- 用法：useradd 用户名
- 设置用户的登录密码：passwd (管理员，普通用户只能更改自己的密码)
	- 用法：passwd 用户名（管理员） passwd 密码（普通用户）
- 查看当前登录的用户：whoami 
- 切换用户: su
	- 用法1：su 用户名  用法2: su 回车（切换到root账户）
	- 特点： 管理员切换到普通用户不需要输入密码，普通用户间切换需要输入密码
- 管理员登录下（#），普通用户登录（$）。显示标识符不相同
